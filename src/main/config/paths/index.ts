/* eslint-disable sort-keys-fix/sort-keys-fix */

export const paths = {
  login: 'login',
  home: 'home'
} as const;

export const apiPaths = {
  auth: '/login',
  register: '/register',
  default: 'default'
};
